Base environment is in `environment.yml`. Main packages are mne, braindecode, moabb and pytorch.

Additionally, need to clone these libraries, to install them to the environment:

`git clone https://github.com/adavoudi/spdnet.git`
`git clone https://gitlab.lip6.fr/schwander/torchspdnet.git`

